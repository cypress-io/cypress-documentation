---
title: 'Components | Cypress Accessibility'
description: 'The accessibility `components` configuration controls how component-related HTML attributes appear in stable violation-target selectors.'
sidebar_label: components
sidebar_position: 45
---

<ProductHeading product="accessibility" />

# components

Use `accessibility.components` to require specific HTML attributes in the **selector strings** shown for accessibility violations, so parallel instances of the same pattern (shared test IDs, repeated layouts) remain distinguishable in Cypress Cloud.

<AccessibilityComponents />
